'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var padZeroes = exports.padZeroes = function padZeroes(width, n) {
  return ('' + n).padStart(width, '0');
};

var getTimeStamp = exports.getTimeStamp = function getTimeStamp() {
  var now = new Date();
  return '[' + padZeroes(2, now.getHours()) + ':' + padZeroes(2, now.getMinutes()) + ':' + (padZeroes(2, now.getSeconds()) + '.' + padZeroes(3, now.getMilliseconds()) + ']');
};